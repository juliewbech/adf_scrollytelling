---
// Henter parametrene fra Astro.props
const { teksttype="brødtekst", font="paragraf", color=""} = Astro.props; 

//Definer den styling (Tailwind classes, der gør sig gældende for alle links
const linkStyling = ""; 
 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
"konsultativ": "text-sm text-italic",
"brødtekst": "md:text-xl my-4",
};

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige skrifttyper.
const fontClasses = {
"sans": "font-paragraf",
"serif": "font-header",
};

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige skrifttyper.
const stylingClasses = {
"white": "text-white",
"black": "text-black",
};
---
<p class:list={[linkStyling, sizeClasses[teksttype], fontClasses[font], stylingClasses[color]]}><slot /></p>